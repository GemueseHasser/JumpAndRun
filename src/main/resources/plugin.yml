name: '${project.name}'
description: '${project.description}'
version: '${project.version}'
load: 'POSTWORLD'
author: 'JustChunks'
authors: ['Jonas0206']
website: '${project.url}'
api-version: '1.8.8'
main: '${project.groupId}.${project.artifactId}.${project.name}'
depend: []
softdepend: []
loadbefore: []
prefix: '${project.name}'

commands:
  dec:
    description: 'Zeigt dir eine Erklärung an!'
    usage: '</command>'
  jarpos:
    description: 'Setzt die Position'
    usage: '</command>'